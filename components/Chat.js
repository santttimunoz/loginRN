import { View, Text } from "react-native";
import {styles} from "../assets/styles/allstyles.js"

export default function Chat(){ 
       return (
        <View style={styles.container}>
        <Text>Estamos en Chat</Text>
        </View>
    )
}
